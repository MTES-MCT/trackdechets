export enum inputType {
  text = "text",
}

export interface BsdCurrentTab {
  isDraftTab?: boolean;
  isActTab?: boolean;
  isToCollectTab?: boolean;
  isFollowTab?: boolean;
  isArchivesTab?: boolean;
}
