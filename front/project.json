{
  "name": "front",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "front/src",
  "projectType": "application",
  "targets": {
    "e2e-ci": {
      "command": "cd front && xvfb-run npx playwright test"
    },
    "e2e": {
      "command": "cd front && npx playwright test && npx playwright show-report"
    },
    "e2e-ui": {
      "command": "cd front && npx playwright test --ui"
    },
    "dsfr": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd front && copy-dsfr-to-public && only-include-used-icons"
      }
    },
    "build": {
      "executor": "@nx/vite:build",
      "dependsOn": ["^build", "dsfr", "generate-tw"],
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/front"
      },
      "configurations": {
        "development": {
          "mode": "development",
          "skipTypeCheck": true
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "dependsOn": ["^build"],
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "front:build",
        "port": 3000,
        "host": "0.0.0.0"
      },
      "configurations": {
        "development": {
          "buildTarget": "front:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "front:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "front:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "front:build:development"
        },
        "production": {
          "buildTarget": "front:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "dependsOn": ["^build", "generate-tw"],
      "options": {
        "passWithNoTests": true,
        "jestConfig": "front/jest.config.js"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["front/**/*.{ts,tsx,js,jsx}"]
      }
    },
    "generate-tw": {
      "command": "tailwindcss build -c front/tailwind.config.js front/src/tailwind.css -o front/src/generated/css/tailwind.generated.css"
    },
    "storybook": {
      "executor": "@nx/storybook:storybook",
      "options": {
        "port": 4400,
        "configDir": "front/.storybook"
      },
      "configurations": {
        "ci": {
          "quiet": true
        }
      }
    },
    "build-storybook": {
      "executor": "@nx/storybook:build",
      "outputs": ["{options.outputDir}"],
      "options": {
        "outputDir": "dist/storybook/front",
        "configDir": "front/.storybook"
      },
      "configurations": {
        "ci": {
          "quiet": true
        }
      }
    },
    "static-storybook": {
      "executor": "@nx/web:file-server",
      "options": {
        "buildTarget": "front:build-storybook",
        "staticFilePath": "dist/storybook/front"
      },
      "configurations": {
        "ci": {
          "buildTarget": "front:build-storybook:ci"
        }
      }
    }
  },
  "tags": []
}
