overwrite: true
schema: "back/src/**/*.graphql"
generates:
  codegen-ui/src/generated/ui.ts:
    plugins:
      - typescript
      - typescript-operations
    config:
      scalars:
        DateTime: string
        URL: string
