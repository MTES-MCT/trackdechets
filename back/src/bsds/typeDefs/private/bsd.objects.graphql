type BsdConnection {
  totalCount: Int!
  pageInfo: PageInfo!
  edges: [BsdEdge!]!
}

type BsdEdge {
  cursor: String!
  node: Bsd!
}

union Bsd = Form | Bsdasri | Bsvhu | Bsda | Bsff | Bspaoh

type CloneId {
  id: ID!
}
