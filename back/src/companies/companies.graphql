type Company {
  siret: String
  name: String
  address: String
  contact: String
  phone: String
  mail: String
}

type Query {
  companyInfos(siret: String!): Company
  favorites(type: FavoriteType!): [Company]
}

enum FavoriteType {
  EMITTER
  TRANSPORTER
  RECIPIENT
}
