type Company {
  siret: String
  name: String
  address: String
  contact: String
  phone: String
  mail: String

  admin: PublicUser
}

type Query {
  companyInfos(siret: String!): Company
  companyUsers(siret: String!): [User]
  searchCompanies(clue: String!): [Company]
  favorites(type: FavoriteType!): [Company]
}

enum FavoriteType {
  EMITTER
  TRANSPORTER
  RECIPIENT
  TRADER
}
