import forms from "./subscriptions/forms";
import { SubscriptionResolvers } from "../../generated/graphql/types";

const subscriptionResolvers: SubscriptionResolvers = {
  forms
};

export default subscriptionResolvers;
