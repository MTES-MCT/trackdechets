generator client {
  provider        = "prisma-client"
  output          = "./generated/prisma"
  previewFeatures = []
  engineType      = "client"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
